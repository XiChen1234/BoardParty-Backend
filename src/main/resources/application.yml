spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board_party?useSSL=false&serverTimezone=UTC
    username: root # 示例账号
    password: 123456 # 示例密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: ""